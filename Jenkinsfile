pipeline {
    agent any

    stages {

        stage("build"){

            steps{
                ehco 'building the application'
            }
        }

        stage("test"){

            steps{
                ehco 'testing the application'
            }
        }

        stage("deploy"){

            steps{
                ehco 'deploying the application'
            }
        }
    }
}